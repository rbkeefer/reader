/*
 * RkSegment.h
 *
 */

#ifndef RKSEGMENT_H_
#define RKSEGMENT_H_

class RkSegment {
public:
	RkSegment();
	RkSegment(RkBoundingRectangle* inBoundingRect, int inSegmentType);
	virtual ~RkSegment();

	RkBoundingRectangle* boundingRectangle;
	int segmentType;
};

#endif /* RKSEGMENT_H_ */
