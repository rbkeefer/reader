// combineTouchingRects.h

#ifndef INCLUDED_COMBINE_TOUCHING_RECTS_H
#define INCLUDED_COMBINE_TOUCHING_RECTS_H

list<BoundingRectangle*> combineTouchingRectangles(list<BoundingRectangle*> inRectList,
                                                   int inTolerance);

list<BoundingRectangle*> combineCloseRects(list<BoundingRectangle*> inRectList,
                                           int inTolerance);

BoundingRectangle* isTouchingRectangle(BoundingRectangle* inRect1,
                                       BoundingRectangle* inRect2,
                                       int inTolerance);

list<BoundingRectangle*> getTouchingRects(BoundingRectangle* inRectangle,
                                          list<BoundingRectangle*> inRectangleList, int inTolerance);

#endif

