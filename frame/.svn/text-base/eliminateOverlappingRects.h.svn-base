// eliminateOverlappingRects.h

#ifndef INCLUDED_ELIMINATEOVERLAPPING_RECTS_H
#define INCLUDED_ELIMINATEOVERLAPPING_RECTS_H

list<BoundingRectangle*> eliminateOverlappingRectangles(list<BoundingRectangle*> inRectList,
                                                        int inTolerance);

list<BoundingRectangle*> eliminateOverlappingRects(list<BoundingRectangle*> inRectList,
                                           int inTolerance);

list<BoundingRectangle*> getOverlappingRects(BoundingRectangle* inRectangle,
                                          list<BoundingRectangle*> inRectangleList, int inTolerance);

BoundingRectangle* isOverlappingRectangle(BoundingRectangle* inRect1,
                                       BoundingRectangle* inRect2,
                                       int inTolerance);

#endif

